import '../../../student/data/models/quiz.dart' as api;
import '../../student/data/models/quiz.dart' as api;

/// We reuse the student Quiz model — the fields are identical.
typedef InstructorQuiz = api.Quiz;
typedef QuizStatus = api.QuizStatus;